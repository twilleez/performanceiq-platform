export function calculateTeamAverages(athletes){const count=athletes.length||1;return{piq:Math.round(athletes.reduce((s,a)=>s+a.piq,0)/count),readiness:Math.round(athletes.reduce((s,a)=>s+a.readiness,0)/count),compliance:Math.round(athletes.reduce((s,a)=>s+a.compliance,0)/count),atRisk:athletes.filter(a=>a.risk!=="low").length}}export function calculateSessionProgress(session){const total=session.exercises.reduce((s,ex)=>s+Number(ex.sets||0),0)||1;const done=session.exercises.reduce((s,ex)=>s+Number(ex.setsCompleted||0),0);return Math.round(done/total*100)}