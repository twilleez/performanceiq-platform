import { STATE } from "./state/state.js";import { loadState, saveState } from "./services/storage.js";import { signInLocal } from "./services/authService.js";import { renderCurrentView } from "./router.js";import { authView } from "./views/auth.js";import { swapExerciseInWorkout, addExerciseToWorkout } from "./features/performanceEngine.js";const app=document.getElementById("app");const bootStatus=document.getElementById("bootStatus");const persisted=loadState();if(persisted)Object.assign(STATE,persisted);function navButton(label,view){return `<button class="${STATE.ui.view===view?"":"secondary"}" data-nav="${view}">${label}</button>`}function shell(content){return `<div class="app"><aside class="sidebar"><div class="brand">PerformanceIQ</div><div class="subbrand">Phase 11 • Major Sports</div><div class="nav">${navButton("Dashboard","dashboard")}${navButton("Team","team")}${navButton("Workouts","workouts")}${navButton("Builder","builder")}${navButton("Videos","videos")}${navButton("Athlete","athlete")}${navButton("Periodization","periodization")}${navButton("Recruiting","recruiting")}${navButton("Setup","setup")}<button class="secondary" data-logout="true">Logout</button></div></aside><main class="main">${content}</main></div>`}function render(){bootStatus.textContent="Booting PerformanceIQ...";if(!STATE.session.loggedIn){app.innerHTML=authView();bindAuth();return}app.innerHTML=shell(renderCurrentView(STATE));bindGlobalUI();saveState(STATE)}function bindAuth(){const btn=document.getElementById("login-btn");if(!btn)return;btn.onclick=()=>{const email=document.getElementById("auth-email").value.trim();const role=document.getElementById("auth-role").value;if(!email)return alert("Enter email.");signInLocal(STATE,email,role);STATE.ui.view=role==="athlete"?"athlete":"dashboard";render()}}function bindGlobalUI(){app.querySelectorAll("[data-nav]").forEach(btn=>btn.onclick=()=>{STATE.ui.view=btn.dataset.nav;render()});app.querySelectorAll("[data-athlete]").forEach(btn=>btn.onclick=()=>{STATE.ui.activeAthleteId=btn.dataset.athlete;STATE.ui.view="team";render()});const logout=app.querySelector("[data-logout='true']");if(logout)logout.onclick=()=>{STATE.session.loggedIn=false;STATE.session.user=null;render()};const seed=app.querySelector("[data-builder='seed']");if(seed)seed.onclick=()=>{document.getElementById("wb-title").value="Major Sport Session";document.getElementById("wb-day-type").value="power";document.getElementById("wb-notes").value="Sport-specific force, speed, and core support."};const save=app.querySelector("[data-builder='save']");if(save)save.onclick=()=>{STATE.builder.title=document.getElementById("wb-title").value.trim();STATE.builder.dayType=document.getElementById("wb-day-type").value;STATE.builder.notes=document.getElementById("wb-notes").value.trim();STATE.builder.athleteId=document.getElementById("wb-athlete").value;const athlete=STATE.roster.find(a=>a.id===STATE.builder.athleteId);if(athlete&&STATE.builder.title){athlete.workouts.unshift({id:`w_${Date.now()}`,title:STATE.builder.title,dayType:STATE.builder.dayType,status:"assigned",notes:STATE.builder.notes,exercises:athlete.workouts[0]?.exercises?.slice(0,5)||[]});alert("Workout saved to local state.");STATE.ui.activeAthleteId=athlete.id;}else{alert("Enter a workout title and athlete.");}render()};app.querySelectorAll("[data-swap]").forEach(btn=>btn.onclick=()=>{const [athleteId,workoutId,exerciseId]=btn.dataset.swap.split("|");STATE.ui.swapTarget={athleteId,workoutId,exerciseId};render()});const close=app.querySelector("[data-swap-close='true']");if(close)close.onclick=()=>{STATE.ui.swapTarget=null;render()};app.querySelectorAll("[data-swap-pick]").forEach(btn=>btn.onclick=()=>{const target=STATE.ui.swapTarget;if(!target)return;const athlete=STATE.roster.find(a=>a.id===target.athleteId);const workout=athlete?.workouts.find(w=>w.id===target.workoutId);if(workout)swapExerciseInWorkout(workout,target.exerciseId,btn.dataset.swapPick);STATE.ui.swapTarget=null;render()});const sportFilter=app.querySelector("#sport-filter");if(sportFilter)sportFilter.onchange=()=>{STATE.ui.selectedSport=sportFilter.value;render()};app.querySelectorAll("[data-drag-add]").forEach(btn=>btn.onclick=()=>{const athlete=STATE.roster.find(a=>a.id===STATE.builder.athleteId)||STATE.roster[0];const workout=athlete?.workouts[0];if(workout){addExerciseToWorkout(workout,btn.dataset.dragAdd);alert("Exercise added to current workout.");render()}});app.querySelectorAll("[data-video]").forEach(btn=>btn.onclick=()=>{alert("Video preview placeholder: connect this button to your hosted demo video file.")});const copy=app.querySelector("[data-copy-profile='true']");if(copy)copy.onclick=async()=>{const text=app.querySelector("pre")?.innerText||"";try{await navigator.clipboard.writeText(text);alert("Profile copied.")}catch{alert("Copy failed on this browser.")}}}render()