export function renderSoloBuilder(container) {
  container.innerHTML = `<div class="view-screen">
    <div class="view-nav-bar"><button class="back-btn" onclick="history.back()">←</button>
    <div class="view-nav-title">BUILDER</div><div></div></div>
    <div style="padding:32px 20px;text-align:center;color:rgba(255,255,255,0.3);font-size:13px;">Solo builder — Phase 15D</div>
  </div>`;
}
