import { getExercise, progressPct } from "../features/trainingEngine.js";
export function sessionView(state){
  const workout = state.workouts.find(w => w.id === state.ui.activeWorkoutId) || state.workouts[0];
  return `<div class="screen"><div class="topbar"><div><div class="muted">Session</div><div class="title-xl">${workout.title}</div></div><div class="pill">${progressPct(workout)}% complete</div></div><div class="card section"><div class="title-md">Session Intent</div><div class="muted section">${workout.notes}</div><div class="muted section">${workout.recoveryCue}</div></div><div class="list section">${workout.exercises.map(ex => { const meta = getExercise(ex.id); return `<div class="exercise-card"><div class="space"><div><div class="title-md">${meta?.title || ex.id}</div><div class="muted small">${meta?.pattern || "-"} · ${meta?.equipment || "bodyweight"}</div></div><div class="row"><button class="secondary sm" data-action="detail" data-ex="${ex.id}">View</button><button class="secondary sm" data-action="video" data-ex="${ex.id}">Video</button><button class="secondary sm" data-action="swap" data-ex="${ex.id}">Swap</button><button class="sm" data-action="open-sets" data-ex="${ex.id}">Log</button></div></div><div class="set-row">${ex.sets.map((s, idx) => `<div class="set-tile${s.done ? ' done' : ''}" data-action="toggle-set" data-ex="${ex.id}" data-set="${idx}"><div class="muted small">Set ${idx+1}</div><div>${s.target}</div><div class="small">${s.done ? "✓ Complete" : "Tap to log"}</div></div>`).join("")}</div></div>`; }).join("")}</div></div>`;
}
