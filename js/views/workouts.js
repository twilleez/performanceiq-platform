export function renderWorkouts(state){const rows=state.roster.flatMap(a=>(a.assigned_workouts||[]).map(w=>({athlete_name:a.name,title:w.title,day_type:w.day_type,status:w.status})));return`<div class="card"><h2>Assigned Workouts</h2>${rows.length?`<table><thead><tr><th>Athlete</th><th>Workout</th><th>Day Type</th><th>Status</th></tr></thead><tbody>${rows.map(w=>`<tr><td>${w.athlete_name}</td><td>${w.title}</td><td>${w.day_type||"-"}</td><td>${w.status||"-"}</td></tr>`).join("")}</tbody></table>`:`<div class="muted">No workouts returned. Check workouts and workout_assignments.</div>`}</div>`}