import { progressPct } from "../features/trainingEngine.js";
export function athleteHomeView(state){
  const workout = state.workouts.find(w => w.id === state.ui.activeWorkoutId) || state.workouts[0];
  const pct = progressPct(workout);
  return `<div class="screen"><div class="topbar"><div><div class="muted">Today</div><div class="title-xl">My Training, My Body, My Day</div></div><div class="pill">${state.athlete.block} · Week ${state.athlete.week}</div></div><div class="grid two section"><div class="card"><div class="space"><div><div class="muted small">Readiness</div><div class="metric">${state.athlete.readiness}</div></div><button class="secondary sm" data-action="open-readiness">Details</button></div><div class="row section"><span class="stat-chip">HRV ${state.athlete.hrv}</span><span class="stat-chip">Sleep ${state.athlete.sleep}h</span><span class="stat-chip">Soreness ${state.athlete.soreness}</span><span class="stat-chip">Battery ${state.athlete.bodyBattery}</span></div><div class="banner section">You’re primed for a high quality session. Push outputs, keep technique clean.</div><div class="row section">${state.athlete.focusAreas.map(x => `<span class="pill">${x}</span>`).join("")}</div></div><div class="card"><div class="space"><div><div class="muted small">Today Session</div><div class="title-lg">${workout.title}</div><div class="muted">${workout.notes}</div></div><div class="metric-sm">${pct}%</div></div><div class="row section"><span class="stat-chip">Weekly Volume ${state.athlete.weeklyVolume}</span><span class="stat-chip">Streak ${state.athlete.streak}</span></div><div class="space section"><div class="muted">${workout.recoveryCue}</div><button data-action="start-session">Start</button></div></div></div><div class="card section"><div class="space"><div class="title-md">This Week</div><div class="row"><span class="pill">${state.athlete.goal}</span><button class="secondary sm" data-action="open-history">History</button></div></div><div class="grid three section">${state.athlete.microcycle.map(d => `<div class="day-card"><strong>${d.day}</strong><div class="muted small">${d.label}</div><div class="muted small" style="margin-top:8px">${d.status}</div></div>`).join("")}</div></div></div>`;
}
