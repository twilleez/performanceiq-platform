function badge(r){return `<span class="badge ${r==="low"?"ok":r==="medium"?"warn":"danger"}">${r}</span>`}export function teamView(state){const active=state.roster.find(a=>a.id===state.ui.activeAthleteId)||state.roster[0];return `<div class="grid two"><div class="card"><h2>Roster</h2><table><thead><tr><th>Name</th><th>Pos</th><th>PIQ</th><th>Readiness</th><th>Load</th><th>ACR</th><th>Risk</th></tr></thead><tbody>${state.roster.map(a=>`<tr><td><button class="ghost" data-athlete="${a.id}">${a.name}</button></td><td>${a.pos}</td><td>${a.piq??"-"}</td><td>${a.readiness??"-"}</td><td>${a.load}</td><td>${a.acr??"-"}</td><td>${badge(a.risk)}</td></tr>`).join("")}</tbody></table></div><div class="card">${active?`<h2>${active.name}</h2><div class="row"><span class="badge">PIQ ${active.piq??"-"}</span><span class="badge">Readiness ${active.readiness??"-"}</span><span class="badge">Load ${active.load}</span><span class="badge">ACR ${active.acr??"-"}</span>${badge(active.risk)}</div><div class="card"><strong>Assigned Workouts</strong>${active.workouts.length?active.workouts.map(w=>`<div class="item"><strong>${w.title}</strong><div class="muted small">${w.dayType} • ${w.status}</div></div>`).join(""):`<div class="item">No assigned workouts.</div>`}</div>`:`<div class="item">No athlete selected.</div>`}</div></div>`}
