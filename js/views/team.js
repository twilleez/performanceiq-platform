function badge(r){return `<span class="badge ${r==="low"?"ok":r==="medium"?"warn":"danger"}">${r}</span>`}export function teamView(state){return `<div class="card"><h2>Roster</h2><table><thead><tr><th>Name</th><th>Sport</th><th>Pos</th><th>PIQ</th><th>Readiness</th><th>Risk</th></tr></thead><tbody>${state.roster.map(a=>`<tr><td><button class="ghost" data-athlete="${a.id}">${a.name}</button></td><td>${a.sport}</td><td>${a.pos}</td><td>${a.piq}</td><td>${a.readiness}</td><td>${badge(a.risk)}</td></tr>`).join("")}</tbody></table></div>`}