export function teamHomeView(state){ return `<div class="screen"><div class="topbar"><div><div class="muted">Team</div><div class="title-xl">${state.team.name}</div></div><div class="pill">Still your training</div></div>${state.team.announcements.map(x => `<div class="banner section">${x}</div>`).join("")}<div class="grid two section"><div class="card"><div class="title-md">Coach Notes</div><div class="list section">${state.team.coachNotes.map(x => `<div class="item">${x}</div>`).join("")}</div></div><div class="card"><div class="title-md">Upcoming Team Events</div><div class="list section">${Object.entries(state.team.events).slice(0,4).map(([d,items]) => `<div class="item"><strong>${d}</strong><div class="muted">${items.join(", ")}</div></div>`).join("")}</div></div></div><div class="grid two section"><div class="card"><div class="space"><div class="title-md">Leaderboard</div><button class="secondary sm" data-action="open-team-tab" data-teamtab="roster">Roster</button></div><div class="section">${state.team.leaderboard.map(x => `<div class="item space"><span>${x.name}</span><span class="muted">${x.metric}</span></div>`).join("")}</div></div><div class="card"><div class="title-md">Athlete Compare</div><div class="list section">${state.team.roster.filter(r => r.role !== "coach").map(r => `<div class="compare-card"><strong>${r.name}</strong><div class="row section"><span class="stat-chip">PIQ ${r.piq}</span><span class="stat-chip">Readiness ${r.readiness}</span></div></div>`).join("")}</div></div></div></div>`; }
export function teamScheduleView(state){ const days=["Mon","Tue","Wed","Thu","Fri","Sat","Sun"]; return `<div class="screen"><div class="topbar"><div><div class="muted">Team Schedule</div><div class="title-xl">Merged Calendar</div></div></div><div class="calendar section">${days.map(d => `<div class="day"><strong>${d}</strong><div class="list section">${(state.team.events[d]||[]).map(x => `<div class="item small">${x}</div>`).join("")}</div></div>`).join("")}</div></div>`; }
export function rosterView(state){ return `<div class="screen"><div class="topbar"><div><div class="muted">Roster</div><div class="title-xl">Contact Card Style</div></div></div><div class="list section">${state.team.roster.map(p => `<div class="card"><div class="space"><div><div class="title-md">${p.name}</div><div class="muted">${p.pos}</div></div><span class="role-badge">${p.role}</span></div><div class="row section"><button class="secondary sm">Message</button><button class="secondary sm">View Stats</button><button class="secondary sm">Compare</button></div></div>`).join("")}</div></div>`; }
export function teamActivityView(state){ return `<div class="screen"><div class="topbar"><div><div class="muted">Team Activity</div><div class="title-xl">Signal, Not Noise</div></div></div><div class="list section">${state.team.activity.map(x => `<div class="card">${x}</div>`).join("")}</div></div>`; }