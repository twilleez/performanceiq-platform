export function teamHomeView(state){ return `<div class="sheet-backdrop" data-action="close-sheet"></div><div class="sheet"><div class="sheet-handle"></div><div class="sheet-body"><div class="brand">Team</div><div class="title" style="font-size:28px">${state.team.name}</div><div class="content" style="padding:10px 0 0"><div class="item">${state.team.announcements[0]}</div><div class="glass card"><div class="card-title">Upcoming</div>${state.team.homeEvents.map(x=>`<div class="item">${x}</div>`).join("")}</div><div class="glass card"><div class="card-title">Quick Actions</div><div class="pill-row"><button class="ghost" data-teamtab="schedule">Schedule</button><button class="ghost" data-teamtab="roster">Roster</button><button class="ghost" data-teamtab="activity">Activity</button></div></div></div></div></div>`; }
export function teamScheduleView(state){ const days=["Mon","Tue","Wed","Thu","Fri","Sat","Sun"]; return `<div class="sheet-backdrop" data-action="close-sheet"></div><div class="sheet"><div class="sheet-handle"></div><div class="sheet-body"><div class="brand">Team Schedule</div><div class="title" style="font-size:28px">Calendar</div><div class="calendar" style="margin-top:14px">${days.map(d=>`<div class="day"><strong>${d}</strong><div class="list" style="margin-top:8px">${(state.team.schedule[d]||[]).map(x=>`<div class="item small">${x}</div>`).join("")}</div></div>`).join("")}</div></div></div>`; }
export function teamRosterView(state){ return `<div class="sheet-backdrop" data-action="close-sheet"></div><div class="sheet"><div class="sheet-handle"></div><div class="sheet-body"><div class="brand">Roster</div><div class="title" style="font-size:28px">Team Contacts</div><div class="content" style="padding:10px 0 0">${state.team.roster.map(x=>`<div class="roster-card"><div class="row" style="justify-content:space-between"><div><div style="font-weight:700">${x.name}</div><div class="muted small">${x.pos}</div></div><div class="pill">${x.role}</div></div>${x.piq ? `<div class="compare"><div class="item small">PIQ ${x.piq}</div><div class="item small">Ready ${x.readiness}</div></div>` : `<div class="muted small" style="margin-top:10px">Coach profile</div>`}</div>`).join("")}</div></div></div>`; }
export function teamActivityView(state){ return `<div class="sheet-backdrop" data-action="close-sheet"></div><div class="sheet"><div class="sheet-handle"></div><div class="sheet-body"><div class="brand">Team Activity</div><div class="title" style="font-size:28px">Signal Only</div><div class="content" style="padding:10px 0 0">${state.team.activity.map(x=>`<div class="item">${x}</div>`).join("")}</div></div></div>`; }
