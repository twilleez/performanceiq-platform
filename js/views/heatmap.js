function cls(v){if(v>=80)return"low";if(v>=65)return"mid";return"high"}export function heatmapView(state){return `<div class="card"><h2>Team Heatmap</h2><table><thead><tr><th>Athlete</th><th>PIQ</th><th>Readiness</th><th>Load</th><th>ACR</th></tr></thead><tbody>${state.roster.map(a=>`<tr><td>${a.name}</td><td><div class="heat ${cls(a.piq)}">${a.piq}</div></td><td><div class="heat ${cls(a.readiness)}">${a.readiness}</div></td><td><div class="heat mid">${a.load}</div></td><td><div class="heat ${a.acr>1.5?"high":a.acr>1.3?"mid":"low"}">${a.acr}</div></td></tr>`).join("")}</tbody></table></div>`}