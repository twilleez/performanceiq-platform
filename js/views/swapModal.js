export function swapModalView(options){
  return `<div class="modal-wrap"><div class="modal"><div class="row" style="justify-content:space-between"><h2>Swap Exercise</h2><button class="secondary" data-swap-close="true">Close</button></div><div class="small muted">Choose another basketball exercise with the same training pattern.</div><div class="list" style="margin-top:12px">${options.length ? options.map(o => `<div class="item"><div class="row"><div style="flex:1"><strong>${o.title}</strong><div class="small muted">${o.pattern}</div></div><button data-swap-pick="${o.id}">Use</button></div></div>`).join("") : `<div class="item">No swap options found.</div>`}</div></div></div>`;
}
