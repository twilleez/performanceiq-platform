export function progressView(state){
  return `<div class="screen"><div class="topbar"><div><div class="muted">Progress</div><div class="title-xl">Depth Over Noise</div></div></div><div class="grid three section">${[["Strength",state.progress.strength],["Conditioning",state.progress.conditioning],["Mobility",state.progress.mobility]].map(([l,p]) => `<div class="card"><div class="metric-sm">${p}%</div><div class="muted">${l}</div></div>`).join("")}</div><div class="grid two section"><div class="card"><div class="title-md">Block Timeline</div><div class="list section">${state.progress.timeline.map(x => `<div class="timeline-item space"><span>${x.week}</span><span class="muted">${x.delta}</span></div>`).join("")}</div></div><div class="card"><div class="title-md">PR History</div><div class="list section">${state.progress.prs.map(pr => `<div class="item"><strong>${pr.label}</strong><div class="muted">${pr.value}</div><div class="spark section"></div></div>`).join("")}</div></div></div><div class="grid two section"><div class="card"><div class="title-md">Insights</div><div class="list section">${state.progress.insights.map(x => `<div class="item">${x}</div>`).join("")}</div></div><div class="card"><div class="title-md">Weekly Notes</div><div class="list section">${state.progress.weeklyNotes.map(x => `<div class="note-card">${x}</div>`).join("")}</div></div></div></div>`;
}
