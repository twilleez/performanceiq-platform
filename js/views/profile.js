export function profileView(state){
  return `<div class="screen"><div class="topbar"><div><div class="muted">Profile</div><div class="title-xl">${state.athlete.name}</div></div></div><div class="grid two section"><div class="card"><div class="title-md">Athlete Identity</div><div class="list section"><div class="item"><strong>Sport</strong><div class="muted">${state.athlete.sport}</div></div><div class="item"><strong>Position</strong><div class="muted">${state.athlete.position}</div></div><div class="item"><strong>Goal</strong><div class="muted">${state.athlete.goal}</div></div><div class="item"><strong>Equipment</strong><div class="muted">${state.athlete.equipment.join(", ")}</div></div></div></div><div class="card"><div class="title-md">Preferences</div><div class="list section"><div class="item"><strong>Units</strong><div class="muted">Imperial</div></div><div class="item"><strong>Notifications</strong><div class="muted">Training reminders on</div></div><div class="item"><strong>Accessibility</strong><div class="muted">Large tap targets enabled</div></div><div class="item"><strong>Meal Plan Add-on</strong><div class="muted">${state.athlete.mealPlanEnabled ? "Enabled" : "Disabled"}</div></div></div></div></div><div class="card section"><div class="title-md">Recovery Notes</div><div class="muted section">${state.athlete.notes}</div></div></div>`;
}
