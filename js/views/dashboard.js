export function dashboardView(state){return `<div class="grid cards"><div class="card"><h3>Team PIQ</h3><div class="timer">${state.summary.piq}</div></div><div class="card"><h3>Avg Readiness</h3><div class="timer">${state.summary.readiness}</div></div><div class="card"><h3>Weekly Load</h3><div class="timer">${state.summary.weeklyLoad}</div></div><div class="card"><h3>Athletes at Risk</h3><div class="timer">${state.summary.atRisk}</div></div></div><div class="grid two"><div class="card"><h2>PerformanceIQ Dashboard</h2><p>Team: ${state.team.name}</p><p>Join Code: ${state.team.joinCode}</p><p>User: ${state.session.user||"-"}</p></div><div class="card"><h2>Coach Alerts</h2><div class="list">${state.alerts.length?state.alerts.map(a=>`<div class="item">${a}</div>`).join(""):`<div class="item">No current alerts.</div>`}</div></div></div>`}
